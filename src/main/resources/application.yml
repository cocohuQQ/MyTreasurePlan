server:
  port: 8080
spring:
  datasource:
    username: coco
    password: coco
    url: jdbc:mysql://localhost:3306/treasure?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.coco.treasure

#showSql
logging:
  level:
    com:
      example:
        mapper : debug
